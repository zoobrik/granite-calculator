# Cloudflare Pages SPA fallback.
# CF Pages rejects /*  /index.html  200 as an "infinite loop" (strips .html → re-matches /*).
# Use explicit route prefixes instead — covers all app routes without triggering the detector.
/c/*          /index.html   200
/category/*   /index.html   200
