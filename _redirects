# Cloudflare Pages SPA fallback.
# CF Pages rejects any rule targeting /index.html (strips .html extension, triggering loop detection).
# Rewrite to / instead — CF Pages serves index.html as the default document for /.
/c/*          /   200
/category/*   /   200
