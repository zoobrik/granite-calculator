# macOS
.DS_Store
._*

# Editor
.vscode/
.idea/
*.swp
*~

# Local artifacts
node_modules/
*.log

# Build output — regenerated by `npm run build` (locally and during the
# Cloudflare Pages deploy). Don't commit; the build script is the source of truth.
dist/

# Audit / scratch (keep these out of the deploy)
audit/
screenshots/
uploads/
